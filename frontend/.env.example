# Database
DATABASE_URL="postgresql://username:password@localhost:5432/carnivore_meal?schema=public"

# Supabase
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Stripe (we'll add these when we get to billing)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Email (we'll add this when we get to auth)
RESEND_API_KEY="re_..."

# Instacart (we'll add this when we get to grocery integration)
INSTACART_API_KEY="your-instacart-key"

# Error tracking (we'll add this when we get to monitoring)
SENTRY_DSN="https://your-sentry-dsn"

# App
NEXTAUTH_SECRET="your-random-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"